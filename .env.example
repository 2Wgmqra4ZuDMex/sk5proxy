# 默认使用本地离线镜像；复制为 .env 后可按需修改。
SK5_IMAGE=sk5proxy:offline
SK5_PROXY_BIND_IP=127.0.0.1
SK5_PROXY_PORT_RANGE=10080-10180

# Docker Hub 发布后可改为（请替换用户名和版本）：
# SK5_IMAGE=<dockerhub-username>/sk5proxy:v1.2.3
