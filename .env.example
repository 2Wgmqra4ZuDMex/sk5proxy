# Docker Hub 已发布的多架构镜像（amd64/arm64）；
# 全新机器 `cp .env.example .env` 后直接 `docker compose pull && docker compose up -d` 即可。
SK5_IMAGE=buffer1705/sk5proxy:v1.0.0
SK5_PROXY_BIND_IP=127.0.0.1
SK5_PROXY_PORT_RANGE=10080-10180

# 固定到某个已发布版本更稳妥；latest 会随新发布变动，不建议生产使用。
# SK5_IMAGE=buffer1705/sk5proxy:latest
